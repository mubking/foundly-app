# Claude Instructions

Always read FOUNDLY_RULES.md before making changes.

This project uses:

- React Native
- Expo SDK 54
- JavaScript
- React Navigation
- StyleSheet

Rules:

- Match the Figma exactly.
- Never redesign the UI.
- Never modify package.json unless I explicitly ask.
- Never upgrade Expo.
- Never rename existing files.
- Never break navigation.
- Build one screen at a time.
- Reuse components whenever possible.
- Keep files under 250 lines by extracting reusable components.
- Before generating code, list the files that will be created or modified.
- Provide complete file contents, not partial snippets.