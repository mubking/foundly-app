{
  "name": "socket",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Standalone Socket.IO real-time service for Foundly. Shares MongoDB and the JWT secret with backend/, but runs as its own process.",
  "main": "server.js",
  "scripts": {
    "dev": "node --watch server.js",
    "start": "node server.js",
    "lint": "eslint ."
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^9.9.1",
    "socket.io": "^4.8.3"
  },
  "devDependencies": {
    "@eslint/js": "^9",
    "eslint": "^9",
    "globals": "^16"
  }
}
