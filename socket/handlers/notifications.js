// Notification push events are not implemented yet — this phase only
// builds connection infrastructure. Reserved for a future phase; not
// wired into handlers/connection.js.
export function registerNotificationHandlers(_io, _socket) {}

export default registerNotificationHandlers;
